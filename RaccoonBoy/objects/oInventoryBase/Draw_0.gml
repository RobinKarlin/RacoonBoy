/// @description Insert description here
// You can write your code in this editor
inventoryx = (oPlayer.x - (sprite_width / 2) );
inventoryy = (oPlayer.y + (sprite_height) );

draw_sprite(sInventoryBase, 0, inventoryx, inventoryy);
